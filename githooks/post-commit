#!/usr/bin/env bash
set -uo pipefail

command -v chezmoi >/dev/null 2>&1 || exit 0

if chezmoi verify >/dev/null 2>&1; then
	exit 0
fi

if ! chezmoi apply; then
	echo "post-commit: chezmoi apply failed (commit kept)" >&2
fi
exit 0
